<%= form_with(model: @recipe_food, url: recipe_recipe_food_path(@recipe, @recipe_food)) do |form| %>
  <%= form.label :food_id, "Selected Ingredient"%>
  <%= form.text_field(:food_id, value: @recipe_food.food.name) %> <br><br>
  <%= form.label "Define quantity for the ingredient "%>
  <%= form.number_field :quantity, min: 1 %><br><br>
  <%= form.submit "Update quantities", class: "button" %>
<% end %>
